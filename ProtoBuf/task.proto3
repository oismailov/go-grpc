syntax = "proto3";

import "fields.proto3";
import "state.proto3";

// RunConds []Cond
// ContinueConds []Cond
// DoneConds []Cond

message TaskBody {
    string id = 1;
    repeated Field Fields = 2;
    map<string, string> Environment = 3;
    State State = 4;
}